// Файл начальных данных.
// Встроенный справочник контактов удалён — контакты теперь загружаются
// исключительно через импорт из Excel (кнопка «Импорт из Excel» в разделе
// «Контакты»). Формат колонок файла: ФИО, Отдел, номер, должность.

// ===== Коллекции схемы v6 =====
// Объявлены глобально, чтобы js/api.js возвращал их на сервер при сохранении:
// клиент отправляет снапшот базы целиком, и незаявленное поле было бы потеряно
// при первой же записи из браузера.
let taskColumnsPerManager = {};   // индивидуальные столбцы: userId -> [столбец]
let activityToColumnMap = {};     // тип активности -> id столбца канбана
let specialNotes = [];            // особые отметки — отдельно от комментариев
let matrices = [];                // заказы матриц: шифр, покрытие, статус, клиент, дата
let clientTransferRequests = [];  // запросы на перенос клиента к другому менеджеру
let chatManagers = [];            // чат менеджеров по продажам
let chatLeads = [];               // чат руководителей
let permissions = [];             // каталог прав: { id, title, group }
let rolePermissions = {};         // матрица доступа: роль -> [права]
