# Локальная конфигурация: здесь хранятся API-ключи внешних сервисов.
# В репозиторий не попадает ни при каких условиях.
config.local.json
config.local.json.tmp

# Служебные файлы
db.json.tmp
node_modules/
*.log

# Активные сессии входов (httpOnly cookie) — служебный файл сервера.
sessions.json
sessions.json.tmp

# Резервные копии базы, которые сервер делает перед миграцией схемы.
db.json.v*.bak

# Скриншоты проверки тёмной темы (временные артефакты проверок).
_shots/
